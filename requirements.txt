Flask>=3.0,<4
Pillow>=10,<13
requests>=2.31,<3
playwright>=1.45,<2
pytest>=8,<9
# 本地画质增强（Swin2SR 超分 / LaMa 去水印）——离线、免 API key。
# 缺这几个也不影响主流程：image_enhance 惰性 import，缺依赖时画质增强优雅跳过。
onnxruntime>=1.17
opencv-python-headless>=4.9
numpy>=1.26
